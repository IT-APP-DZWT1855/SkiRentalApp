@model List<SkiRental.Models.OrderItem>

@{
    ViewBag.Title = "UserOrders";
}

<h2>Zamówienia użytkownika</h2>

@if (Model.Count == 0)
{
    <div class="alert alert-warning" role="alert"><p>Użytkownik nie posiada żadnych zamówień.</p></div>
}